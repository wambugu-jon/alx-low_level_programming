#ifndef SUM_H
#define SUM_H

/**
 * SUM - macro function like for sum
 *
 *@x: First argument.
 *@y: Second argument.
 * Desc: Header file that defines a function-like macro SUM(x, y)
 *       that computes the sum of the numbers x and y.
 */

#define SUM(x, y) ((x) + (y))

#endif
